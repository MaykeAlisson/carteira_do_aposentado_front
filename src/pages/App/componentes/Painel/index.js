import React from "react";
import {Box} from "@mui/material";

const Page = () => {

    return (
        <Box sx={{bgcolor: '#cfe8fc', height: '100vh'}}>
            <span>Painel</span>
        </Box>
    );
}

export default Page;
